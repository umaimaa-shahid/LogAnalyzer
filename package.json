{
  "name": "log-analyzer",
  "version": "1.0.0",
  "description": "Web server log analyzer — CLI report + web dashboard",
  "main": "index.js",
  "bin": {
    "log-analyzer": "./index.js"
  },
  "scripts": {
    "start": "node index.js",
    "generate": "node scripts/generate-logs.js"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.1.0",
    "express": "^4.18.2"
  }
}
